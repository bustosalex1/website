---
interface Props {
    date: Date;
}

const { date } = Astro.props;
---

<div class="text-xs font-mono">
    {
        date.toLocaleDateString("en-US", {
            year: "numeric",
            month: "2-digit",
            day: "2-digit",
        })
    }
</div>
