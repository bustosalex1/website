/**
 * Aliases for light and dark themes. There's probably a better way to do this but the idea is that
 * you can use these wherever you need to instead of putting `bumblebee` in a bunch of places and
 * then deciding that you don't like the `bumblebee` theme and having to go through and change
 * everything.
 */
export const modes = {
    light: "bumblebee",
    dark: "dracula",
};
